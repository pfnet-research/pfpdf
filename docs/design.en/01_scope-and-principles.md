# 1. Scope and basic principles

## 1.1 What pfpdf is

pfpdf is a command-line tool that converts Markdown documents to PDF through HTML. Its primary goal is to let anyone with Node.js and npm generate a PDF with a single command, without first cloning the repository:

```bash
npx @pfnet-research/pfpdf@latest --input foo --output foo.pdf
```

In CI and in long-lived runbooks, pin the version instead of using `latest`.

```bash
npx --yes @pfnet-research/pfpdf@0.1.0 --input foo --output foo.pdf
```

## 1.2 Goals for `v0.1.0`

- Work with only Node.js and bundled dependencies
- Make `npx @pfnet-research/pfpdf` the only official installation path for the user-facing CLI
- Require no external Python installation
- Support all official GitHub Flavored Markdown (GFM) syntax in principle, including the underlying CommonMark syntax
- Reliably convert Japanese strong emphasis containing full-width punctuation and brackets, such as `これは**「重要」**です`, into `<strong>` even without surrounding whitespace
- Support inline and block raw HTML, preserving layout flexibility that Markdown alone cannot express
- Support cover pages, tables of contents, page breaks, math, code highlighting, and local images
- Default to redistributable bundled fonts, and load host fonts only when explicitly permitted
- Offer several bundled templates (`academic`, `book`, `compact`, `default`, `notebook`, `pfn`, and `technical`) while allowing users to inject non-bundled assets such as logos
- Allow runtime configuration through CLI arguments and environment variables, with CLI arguments always taking precedence for the same setting
- Never modify the input source directory, and clean up temporary files on normal exit and on catchable abnormal exit
- Never report success for a partial artifact; return a nonzero exit code on failure
- Parse each Markdown file independently so that file boundaries do not change the meaning of GFM blocks, and resolve static local resources through a renderer-neutral manifest
- Apply finite deadlines to renderer preparation, document readiness, and rendering; never wait indefinitely for stalled browser downloads, scripts, or browsers
- Treat macOS aarch64 and Linux x86_64 as primary supported platforms, and Windows x86_64 and Linux aarch64 as additionally supported platforms, verifying all four environments in CI

## 1.3 Non-goals for `v0.1.0`

- Generating byte-identical PDFs across all OSes
- Providing a sandbox that safely executes untrusted Markdown or raw HTML
- Simultaneously publishing to PyPI, to Homebrew, or as a standalone binary
- GUI preview or a resident watch mode
- Quality guarantees for PDF/X, CMYK, or press-ready preflight
- Fully reproducing GitHub.com-specific post-processing that requires GitHub repository context, such as mentions and issue / pull request references
- Matching GitHub.com's HTML sanitization. pfpdf targets trusted documents and preserves raw HTML
- Producing distributable, single-file HTML. Generated HTML is used only as renderer input and for testing
- Guaranteed reclamation of workspaces or browser processes at the instant of `SIGKILL`, power loss, or an OS crash
- Arbitrating among multiple processes targeting the same output destination, and automatic crash recovery of sibling temporary files left in the output directory after `SIGKILL` or power loss
- Pre-discovering arbitrary local paths, DOM, or network responses generated by scripts at runtime and exposing them to the renderer
- Guaranteeing the integrity, response time, content snapshotting, or failure classification of remote resources
- Providing a Nix Flake, a Nix package, or a Nix-specific reproducible environment

## 1.4 npx-first

General users receive pfpdf as an npm package. The `bin` field in `package.json` registers the `pfpdf` executable, so users can launch it with `npx` or `npm exec` without a global installation.

- The supported Node.js range is determined by the requirements of the pinned Vivliostyle CLI, and the same constraint is used in `package.json` `engines`, the startup check, and CI
- Because `npx` does not enforce npm's `engines` field, the CLI entry point avoids syntax that would fail to parse on older Node.js versions. It checks the runtime version immediately at startup and exits with code `1` and a clear diagnostic when the version is unsupported
- The npm package includes the CLI, the conversion logic, templates, CSS, MathJax, highlight.js, and the bundled fonts
- Browser acquisition and cache management are delegated to the pinned Vivliostyle CLI and the standard behavior of its dependency, the Puppeteer-family browser manager. pfpdf does not implement its own downloader, cache locking, partial-download recovery, or pruning
- The README explicitly notes that a large browser download may occur on first run
- `--browser-path` and `PFPDF_BROWSER_PATH` provide an escape hatch for selecting an existing compatible browser explicitly
- On Linux, the browser archive alone does not provide all required shared libraries, so supported distributions and required packages are listed in chapter 07 and in the tutorial's troubleshooting chapter. `--doctor` reports missing libraries where possible, and no implicit installation with root privileges is performed

### Supported environments

| OS / architecture | Support level | Notes |
|---|---|---|
| macOS aarch64 | Primary support | Apple Silicon. Node tests and PDF generation with a real browser are verified in CI |
| Linux x86_64 | Primary support | Node tests and PDF generation with a real browser are verified in CI |
| Windows x86_64 | Additional support | npx execution from PowerShell and PDF generation with a real browser are verified in CI |
| Linux aarch64 | Additional support | Node tests and PDF generation with a real browser are verified in CI |

## 1.5 The CLI is distributed only through npm

For `v0.1.0`, npm and npx provide the only official installation path for the user-facing CLI. `package-lock.json` pins the source and CI dependency tree, while the tarball's `npm-shrinkwrap.json` pins the published CLI's dependencies. Together with the verified Node.js range, these files make dependency resolution reproducible.

## 1.6 Built-in features work offline once the browser is acquired

The files required for math and code highlighting are not fetched from a CDN; fixed versions are included in the package.

- The npx variant uses the network for the initial download of the npm package and the browser
- Once the npm package and browser are cached, a standard document that uses only pfpdf's built-in features can be converted to PDF without network access
- For remote images, stylesheets, and scripts written by the user, network access is the user's own responsibility
- Typesetting with the bundled MathJax and code highlighting with highlight.js complete as build-time transforms before HTML assembly, and integration tests confirm that Vivliostyle paginates only the transformed HTML
- Initialization or transformation failures in bundled MathJax or highlight.js, and rejected promises registered through the document-readiness contract, prevent a successful build. pfpdf does not classify or guarantee timeout, DNS, TLS, or HTTP failures for user-specified remote resources; those failures and their effects on output stability remain the user's responsibility
- A common deadline, 300 seconds by default, covers browser verification and acquisition, readiness, and PDF rendering, post-processing, and structural inspection, and may be overridden explicitly only for documents that need it. There is no way to make the timeout unlimited

## 1.7 Configuration is limited to CLI arguments and environment variables

There is no project configuration file. Store recurring repository settings as CLI arguments or environment variables in Makefiles, npm scripts, CI workflows, or similar automation.

The configuration precedence is as follows.

```text
Explicit CLI arguments > environment variables > built-in defaults
```

If the same setting appears both on the CLI and in an environment variable, the CLI value always wins regardless of the kind of value. Explicit boolean negations such as `--no-toc` / `--no-host-fonts` take precedence over environment variables as the CLI value `false`. Dedicated CLI reset flags for the logo, additional font directories, browser path, and workspace retention can disable the corresponding environment variable for a single invocation. To restore a setting without a reset flag to its built-in default, remove the unneeded environment variable from the execution environment or specify the default explicitly on the CLI.

## 1.8 Contract between the canon and its translations

The repository's long-lived specification, design, and usage documentation is consolidated into the following four documents.

| Document | Role | Language status |
|---|---|---|
| `docs/design.ja/` | Implementation policy, architecture, compatibility, security, design decisions | Japanese canon |
| `docs/tutorial.ja/` | Usage examples from setup through GFM, raw HTML, math, templates, and the CLI | Japanese canon |
| `docs/design.en/` | English translation of the design | Translation subordinate to the Japanese version |
| `docs/tutorial.en/` | English translation of the tutorial | Translation subordinate to the Japanese version |

- The Japanese and English versions use the same numbering, file names, and chapter structure so that their correspondence can be checked mechanically
- If the Japanese and English versions disagree on the interpretation of the specification, the Japanese version prevails and the English version is corrected
- A pull request that changes the Japanese version updates the corresponding English files in the same pull request
- The README is limited to a short onboarding entry point with pointers to each canon and its translation, and does not duplicate detailed specifications
- As an exception to the Japanese canon, all `AGENTS.md` files in the repository are written in English. The substantive design rationale for agent-facing conventions lives in the design docs; `AGENTS.md` contains only concise working rules and references to the canon
